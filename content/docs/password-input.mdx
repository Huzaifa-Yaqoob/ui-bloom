---
title: "Password Input"
description: "A password input field with a visibility toggle icon for forms."
---

import { PasswordInputForm } from "@/components/forms/password-input";
import PackageInstall from "@/components/docs/PackageInstall";
import PreviewTabs from "@/components/docs/PreviewTabs";
// import PreviewTabsSourceCode from "!!raw-loader!@/components/forms/password-input";
import { DynamicCodeBlock } from "fumadocs-ui/components/dynamic-codeblock";
import config from "config.ts";

<Callout title="Usage Restriction">
  This component should only be used inside a form component.
</Callout>

## Preview

{<PreviewTabs component={<PasswordInputForm />} fileUrl={"/components/forms/password-input.tsx"} name={"password-input"}/>}

## Installation

{<PackageInstall name={"password-input"}/>}

## Usage

{<DynamicCodeBlock lang="typescript"
code={`import { PasswordInput } from "@/components/ui/password-input"`} />}

{<br />}

{<DynamicCodeBlock
lang="typescript"
code={`  <FormField
      control={form.control}
      name="password"
      render={({ field }) => (
        <FieldRenderer label="Password">
          <PasswordInput placeholder="password" {...field} />
        </FieldRenderer>
      )}
    />`}
/>}

## Dependencies

This component depends on the `Form` component from shadcn/ui. Ensure you have it installed and set up correctly.

[View Form Component Documentation](https://ui.shadcn.com/docs/components/form)
