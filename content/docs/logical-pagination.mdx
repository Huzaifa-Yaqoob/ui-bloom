---
title: 'Logical Pagination'
description: 'A smart pagination component that dynamically displays page numbers based on the current page, total pages, and screen size. It adapts to different viewports using breakpoints and optionally shows previous/next controls for better navigation.
'
---

import { PaginationView } from '@/components/sections/PaginationView';
import { PaginationViewCode } from '@/components/code/sections/PaginationViewCode';
import PackageInstall from '@/components/docs/PackageInstall';
import PreviewTabs from '@/components/docs/PreviewTabs';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Callout } from 'fumadocs-ui/components/callout';

## How To Use

<Steps>
  <Step>

    ### Install

    Run the following command in terminal to add these components in your project.

    <PackageInstall name={'logical-pagination'} />

  </Step>
  <Step>
    ### Import

    You can now import this component anywhere you need to use it.

    <DynamicCodeBlock lang={'tsx'} code={`import { LogicalPagination } from @/components/ui/logical-pagination';`}/>

  </Step>
</Steps>

## Preview

<PreviewTabs
  component={<PaginationView />}
  code={PaginationViewCode}
  name={'logical-pagination'}
/>

## Accepted Props

## Props

<TypeTable
  type={{
    size: {
      description:
        'Object containing the number of visible page numbers for different breakpoints (sm, md, lg, xl, 2xl)',
      type: 'Partial<typeof BreakPoints>',
      default: '{ sm: 5 }',
    },
    totalPages: {
      description: 'Total number of pages',
      type: 'number',
      required: true,
    },
    currentPage: {
      description: 'Current active page number',
      type: 'number',
      required: true,
    },
    isPrev: {
      description: 'Whether to show the previous page button',
      type: 'boolean',
      default: 'true',
    },
    isNext: {
      description: 'Whether to show the next page button',
      type: 'boolean',
      default: 'true',
    },
    onPageChange: {
      description: 'Callback function called when page is changed',
      type: '(page: number) => void',
      required: true,
    },
    className: {
      description: 'Additional CSS classes to apply to the container',
      type: 'string',
    },
    PreviousComponent: {
      description: 'Custom component for the previous button',
      type: 'FC<ComponentProps<"button">>',
      default: 'DefaultPrevious',
    },
    NextComponent: {
      description: 'Custom component for the next button',
      type: 'FC<ComponentProps<"button">>',
      default: 'DefaultNext',
    },
    GapPlaceholderComponent: {
      description: 'Custom component for the ellipsis/gap',
      type: 'FC',
      default: 'DefaultGapPlaceholder',
    },
    PageNumberComponent: {
      description: 'Custom component for the page numbers',
      type: 'FC<PageNumberProps>',
      default: 'DefaultPageNumber',
    },
  }}
/>
