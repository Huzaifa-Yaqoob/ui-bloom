---
title: 'Field Renderer'
description: 'The Field Renderer component is a reusable wrapper for input fields within a form. It helps reduce repetitive code by abstracting commonly used form field structures into a separate, maintainable component. This promotes cleaner code and improves the overall organization of your form logic.'
---

import FieldRendererForm from '@/components/forms/field-renderer-form';
import PackageInstall from '@/components/docs/PackageInstall';
import PreviewTabs from '@/components/docs/PreviewTabs';
import code from '@/components/code/forms/field-renderer-form-code';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Callout } from 'fumadocs-ui/components/callout';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';

![Test](/assets/field_renderer_desktop.png)

## How To Use

Using the Field Renderer component is simple. After installation, import the component and use it within the render function of the FormField component provided by shadcn/ui,<br/>
This helps streamline your form structure by centralizing the rendering logic of form fields into one consistent component.

<Callout title="Usage Restriction">
  This component is designed to be used exclusively within a FormField
  component. It will not function correctly outside of that context.
</Callout>

<Steps>

<Step>
### Install

Run the following command in your terminal to install the component:

<PackageInstall name={'field-renderer'} />

</Step>
<Step>
### Import

Use the following code to import FieldRenderer into any component:

<DynamicCodeBlock
  lang={'tsx'}
  code={`import { FieldRenderer } from '@/components/ui/field-renderer';`}
/>
</Step>
<Step>
### Use

You can now easily use this component in your code.

<DynamicCodeBlock
  lang={'tsx'}
  code={`<FormField
  control={form.control}
  name="username"
  render={({ field }) => (
    <FieldRenderer label="Username">
      <Input placeholder="username" {...field} />
    </FieldRenderer>
  )}
/>`}
/>
</Step>
</Steps>

<Callout>
  If your form structure and styling remain consistent throughout the project,
  it's recommended to apply your custom styles directly within the Field
  Renderer component itself. This approach eliminates the need to repeatedly
  pass styling props every time you use it, making your code cleaner and more
  maintainable.
</Callout>

### Preview

<PreviewTabs
  component={<FieldRendererForm />}
  code={code}
  name={'field-renderer'}
/>

## Accepted Props

This component supports all the props available to the `FormItem` component. In addition, it provides the following extra props:

<TypeTable
  type={{
    label: {
      description: 'The label displayed above the input field.',
      type: 'React.ReactNode',
      default: 'custom component',
    },
    description: {
      description: 'Optional description text below the input field.',
      type: 'React.ReactNode',
      default: 'custom component',
    },
    children: {
      description: 'The input element or any additional children.',
      type: 'React.ReactNode',
      default: '',
      required: true,
    },
  }}
/>

## Dependencies

- **Form:** This component relies on the `Form` component from `shadcn/ui` for proper integration with form handling and submission.
  [View Form Documentation](https://ui.shadcn.com/docs/components/form)
